apiVersion: networking.istio.io/v1alpha3

kind: Gateway

metadata:
   name: weatherapp-ingress

spec:
   selector:
      istio: ingressgateway
   servers:
   - hosts:
      - "weatherapp.157.230.194.92.nip.io"
     port:
        number: 80
        protocol: HTTP
        name: HTTP